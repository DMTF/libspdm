CONFIG_I2C=y
CONFIG_I2C_TARGET=y
CONFIG_MCTP_I2C_GPIO_TARGET=y

CONFIG_I2C_MCUX_LPI2C_BUS_RECOVERY=y

# This board has an RTC, so libspdm reads the time from it rather than
# from a callback (CONFIG_LIBSPDM_WALLCLOCK_RTC is picked up by default).
CONFIG_RTC=y
